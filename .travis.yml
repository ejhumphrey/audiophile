sudo: false

addons:
  apt:
    packages:
    - sox
    -

cache:
  directories:
  - $HOME/env

language: python

notifications:
  email: false

before_script:
    - "apt-get install -y -q install ffmpeg"

python:
    - "2.7"
    - "3.4"
    - "3.5"

install:
    - pip install pytest pytest-cov
    - pip install coveralls
    - pip install -e ./

script:
    - python --version
    - py.test -vs --cov=claudio .

after_success:
    - coveralls
    - pip uninstall -y claudio

after_failure:
    - pip uninstall -y claudio
